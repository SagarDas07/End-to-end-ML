# End-to-end-ML

It's an end to end ML Project pipeline

1. Create a new virtual env and
   command - `python -m venv newenv`

2. Activate newenv
   command - `newenv\Scripts\activate`

3. Install requirements
   command - `pip install -r requirements.txt`